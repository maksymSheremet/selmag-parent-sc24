spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/catalogue
    username: catalogue
    password: catalogue
  flyway:
    schemas:
      - public
      - catalogue
  jpa:
    show-sql: true

server:
  port: 8082

logging:
  level:
    sql: info